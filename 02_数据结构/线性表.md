# 顺序表

## 形式

用一组地址连续的存储单元，依次存放线性表中的数据元素。存放的数据按顺序一个挨着一个。

## 优缺点

只要知道下标，就能立即访问对应元素。

但在指定位置操作时，需要大片移动后续元素。

## 实现

```c
#define MAXSIZE 100
typedef struct {
    int data[MAXSIZE];
    int length;    // 当前长度
} SeqList;
```



利用当前长度和元素的一一对应来进行增删改查。

# 单向链表

# 双向链表



