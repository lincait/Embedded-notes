# 顺序表

## 形式

用一组地址连续的存储单元，依次存放线性表中的数据元素。存放的数据按顺序一个挨着一个。

## 优缺点

只要知道下标，就能立即访问对应元素。

但在指定位置操作时，需要大片移动后续元素。

## 实现

```c
#define MAXSIZE 100
typedef struct {
    int data[MAXSIZE];
    int length;    // 当前长度
} SeqList;
```



利用当前长度和元素的一一对应来进行增删改查。

# 单向链表

## 说明

链表在内存中不要求连续存储，增删高效。

**每个节点都有自己的数据域、指针域以及节点自身的地址。**只要通过给节点的指针域赋值，让各个节点连接起来，后续就可以通过移动一个头指针来访问。但要访问前面的只能从头再来。

**因此头指针作为链表入口要固定不动，增删改查时创建另外的指针来操作，避免丢失链表。**除非要改变头节点本身。

## 实现

### 定义

```c
typedef struct Node {
    int data;           // 数据域
    struct Node* next;  // 指针域，指向下一个节点
} Node;
```

### 基本操作

```c
void func(Node *cur)		
{
    Node *newnode = malloc(sizeof(Node));	//创建新节点
    
    cur = cur->next;		//向后移动，遍历
    
    //在cur后面插入新节点newnode
    newnode->next = cur->next;	
    cur->next = newnode;
    
    //删除cur的后继节点，找到要删的前驱就要停下操作
    Node *tmp = cur->next;	
    cur->next = tmp->next;
    free(tmp);
}
```

## 有头和无头

有头：首地址`head`指向头节点，`head->next`才指向第一个有效节点。

无头：首地址`head`直接指向第一个有效节点。

由上述区别导致两种链表在操作实现上有所不同，尤其在边界。注意当函数需要修改外部头指针**本身**的指向时，要在函数返回值把地址返回或用二级指针。

## 单向循环链表

尾节点的`next`指向头节点，无头时指向第一个有效节点。

尾节点判断：`node->next == head`。

创建单向循环链表：先建一个节点，让它的`next`指向自己，后续的节点用插入操作。

# 双向链表

## 说明

每个节点包含数据、指向前一个节点的指针、指向下一个节点的指针。

## 实现

### 定义

```c
typedef struct Node
{
    int data;
    struct Node *prev;	//指向前驱
    struct Node *next;	//指向后继
}Node;
```

为了通用性，可以把头节点单独定义，改成：

```c
typedef struct Node
{
    struct Node *prev;	
    struct Node *next;	
    char data[1];		//占位符，让节点的数据紧跟在指针之后，需要注意内存对齐。C99用data[]更好。
}Node;

typedef struct
{
    int size;			//存放节点的数据域所占的空间，由用户决定
    struct Node head;	//头节点
}LIST;

//这种写法申请时要加上size
newnode = malloc(sizeof(Node) + LIST->size);
```

在实际中，往往采用通用性更强的**`linux`内核链表**（双向循环），核心内容：

- 实现上：链表结构体不含数据，只有两个指向前后的指针；
- 使用上：在想要组织成链表的数据结构体内嵌入一个头节点，使数据和节点对应上；
- 实现上：通过`container_of`宏从链表指针反推整个宿主结构体的起始地址，用这个地址达到访问数据的目的。

这种操作把**链表结构操作**和**数据内容访问**彻底分开。

### 基本操作

```c
//以下为双向循环链表操作，非循环时注意判断前驱或后继为空的情况
void func(Node *cur)		
{
    //遍历,向前或向后
    cur = cur->next;
    cur = cur->prev;
    
	//在cur后面插入新节点newnode,如果是在cur前面插入类似的就只要改前两行
    newnode->prev = cur;			
    newnode->next = cur->next;	
    newnode->prev->next = newnode;
    newnode->next->prev = newnode;
    
    //删除节点cur
    cur->prev->next = cur->next;
    cur->next->prev = cur->prev;
    free(cur)；

}
```

## 有头和无头

区别同单向链表，一般使用有头。

## 双向循环链表

头节点的前驱指向尾节点，尾节点的后继指向头节点。

实际工程最常用**有头双向循环链表**。工程常用判空：`head->next == head && head->prev == head`
