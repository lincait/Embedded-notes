# 说明

- `shell`是一个命令行解释器，它接收应用程序/用户命令，然后调用操作系统内核。

- 一般情况下在终端输入命令系统就会立刻执行，如果需要重复执行大量命令，就可以把命令写成`shell`脚本，一次性运行。

- 脚本开头需要声明解释器位置。
- 脚本可以用`bash`或`sh`命令执行，也可以直接输入脚本执行（需要有执行权限）。

# 变量

## 系统预定义变量

`HOME、PWD、SHELL、USER`等都是系统预定义变量，通过`$`获取变量的值

## 自定义变量

